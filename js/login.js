const input = document.querySelector('.login_input');
const button = document.querySelector('.login_button');

const validacaoInput = (event) => {
    
}

input.addEventListener('input', validacaoInput);